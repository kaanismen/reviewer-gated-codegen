[pytest]
testpaths = tests
# tests/manual/ elle çalıştırılan ölçüm betikleridir: konteyner ayrıcalığı ve
# gerçek kaynak tüketimi gerektirir, otomatik pakete girmez.
norecursedirs = tests/manual workspaces
addopts = -q --strict-markers
